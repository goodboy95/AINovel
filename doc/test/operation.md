# 操作步骤（端到端）

1. **登录**
   - 打开前端 `http://localhost`，使用默认账号 `admin / password` 登录。
   - 首次登录后首页按钮进入工作台、素材库、世界构建。

2. **故事与大纲**
   - 工作台 → “故事构思”输入灵感点击生成（调用 `/conception`），故事将出现在“故事管理”列表。
   - 在“故事管理”选择故事，可查看角色卡；需要时在右上角编辑/删除。
   - 切到“大纲工作台”，选择故事，新建大纲；点击“生成章节”生成占位章节，编辑章节/场景后点“保存大纲”。

3. **稿件**
   - 工作台 → “小说创作”：选择故事与大纲，点击“新建稿件”；选中场景后使用正文编辑器填写内容，点击“保存”。
   - 可触发“自动生成场景”按钮（示例接口），并在侧边栏查看角色变化日志。

4. **素材库**
   - 页面 `/materials`：在“创建素材”填写标题/正文/标签提交；列表自动刷新。
   - “上传文件”选择 TXT → 开始上传 → 轮询状态完成后，待审列表会出现该素材；在“素材审核”中批准或驳回。
   - 列表中可编辑/删除素材，查重合并、查看引用历史（当前为空）。
   - 工作台“素材检索”标签可直接搜索同一后端的素材。

5. **世界构建**
   - 页面 `/worlds`：点击“新建世界”生成草稿；左侧选择世界后编辑元信息与模块字段，点击“保存草稿”。
   - “发布版本”按钮触发生成/发布流程，可在“生成进度”状态中看到模块状态（示例逻辑标记为 COMPLETED）。

6. **设置与提示词**
   - 页面 `/settings`：在“模型接入”保存 Base URL/Model/API Key，点击“测试连接”。
   - “工作区提示词”/“世界观提示词”标签可编辑并保存模板；帮助按钮跳转至提示词帮助页查看变量/函数说明。

7. **登出**
   - 头部用户菜单选择退出，或清除浏览器 LocalStorage 中的 `token`。
