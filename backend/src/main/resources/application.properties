# Spring Boot Properties
server.port=8080
spring.application.name=ainovel

# Database Properties
spring.datasource.url=jdbc:mysql://43.136.95.117:3306/ainovel?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=girlfriend
spring.datasource.password=v0cB32ZgwPXHE4whxbKipNg6Gb4yY60s
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Encryption Key (Must be 16, 24, or 32 bytes long)
app.encryption.key=a-very-secure-secret-key-123456

# JWT Settings
app.jwt.secret=another-very-long-and-secure-secret-key-for-jwt-generation
app.jwt.expiration-ms=86400000

# Allow circular references (as a last resort)
spring.main.allow-circular-references=true

# To run in development mode, uncomment the following line
spring.profiles.active=dev

# SPA redirect URL (removed, handled by WebConfig)


# Use legacy path matching strategy to support SPA routing patterns
spring.mvc.pathmatch.matching-strategy=ant_path_matcher


# AI Provider Models
openai.model.default=gpt-4-turbo
claude.model.default=claude-3-opus-20240229
claude.model.validation=claude-3-haiku-20240307
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash-latest:generateContent


# Proxy Configuration
proxy.enabled=true
proxy.host=127.0.0.1
proxy.port=7890

# CORS Configuration
cors.allowed-origins=http://localhost:8080
